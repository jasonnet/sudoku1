set up sudoku.jasoncrawford.com.

done: problem: right now the hintcells are not transparent but the surrounding .empty-cell is so the result is striping a row or column or block is selected.

done: for hinting can we use background color instead of characters so font size does not impact layout?

done: establish no hints until one hits hints button.   Then gradually add hints.  And to clear hints, just repeat until all hints listed and then hit it one more time to have no hints.

eliminate the clear button ? 

create a blank game that someone can then populate and save.  This can help with solving sudokus found elsewhere.

understand how the 1.1.1 game has just one correct choice.  how can one quickly spot the contradiction in any other choice?

come up with games that engineered to have a single solution and a fairly achieable path to win.

